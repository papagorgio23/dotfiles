[user]
    name = Jason Lee
    email = jason@aisportsfirm.com
    username = papagorgio23

[core]
	editor = code --wait

[web]
    browser = google-chrome

[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true